# GraduationProject_SmartCity_Backend

- Created Rest API to do crud operation using express.js .
- We have also connected Mongodb and uses Mongoose ORM. 
- start by npm i to install your node modules
